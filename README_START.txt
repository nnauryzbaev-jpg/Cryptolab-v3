CryptoLab Final Clean Build

Run via a local HTTP server, for example:
  python -m http.server 8000
Then open http://localhost:8000/

This clean build keeps only the active JSON-native platform files. Legacy, duplicate, and unused migration artifacts were removed.
